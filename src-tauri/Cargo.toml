[package]
name = "zalo-of-linux"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
tauri = { version = "2.0.0", features = [] }

[build-dependencies]
tauri-build = { version = "2.0.0", features = [] }
